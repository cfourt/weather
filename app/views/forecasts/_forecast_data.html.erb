<% s_data = forecast.serialized_current_weather_data %>

<div class="lead">
  <%= s_data.name %>, <%= s_data.region %> <%= forecast.zipcode %>
  <p><%= s_data.country %></p>
  <p class="font-monospace fs-6"><%= s_data.latitude %>,<%= s_data.longitude %></p>
</div>

<div class="border border-2 rounded mt-5 p-4">

  <div class="row align-items-start">
    <div class="col">
      <div class="row">
        <div class="col-lg-6">
          <%= content_tag :h1, "#{s_data.temp_f}°", class: 'display-1' %>
        </div>

        <div class="col col-lg-6">
          <%= content_tag :span, s_data.condition['text'], class: 'display-6 align-middle' %>
          <%= image_tag(
                s_data.condition['icon'],
                alt: s_data.condition['text']
              ) %>
        </div>
      </div>
    </div>
    <div class="col my-auto mx-auto">
      <%= content_tag :p do %>
        <strong>🌧 Precipitation:</strong> <%= "#{s_data.precip_in} in" %>
      <% end %>
      <%= content_tag :p do %>
        <strong>💧Humidity:</strong> <%= "#{s_data.humidity}" %>
      <% end %>
      <%= content_tag :p do %>
        <strong>🌬️ Wind:</strong> <%= "#{s_data.wind_mph} mph #{s_data.wind_dir}" %>
      <% end %>
    </div>

  </div>
</div>
